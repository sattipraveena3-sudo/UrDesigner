import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),title:text('title').notNull(),document:text('document').notNull(),revision:integer('revision').notNull().default(1),updated:integer('updated').notNull()},t=>[index('projects_owner_updated').on(t.owner,t.updated)]);
export const revisions=sqliteTable('revisions',{id:text('id').primaryKey(),project:text('project').notNull(),owner:text('owner').notNull(),revision:integer('revision').notNull(),document:text('document').notNull(),created:integer('created').notNull()},t=>[index('revisions_project_owner').on(t.project,t.owner),uniqueIndex('revision_unique').on(t.project,t.revision)]);
export const connections=sqliteTable('connections',{id:text('id').primaryKey(),owner:text('owner').notNull(),provider:text('provider').notNull(),model:text('model').notNull(),endpoint:text('endpoint'),ciphertext:text('ciphertext').notNull(),updated:integer('updated').notNull()},t=>[index('connections_owner').on(t.owner)]);
export const assets=sqliteTable('assets',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),size:integer('size').notNull(),created:integer('created').notNull()},t=>[index('assets_owner').on(t.owner)]);
export const chats=sqliteTable('chats',{id:text('id').primaryKey(),owner:text('owner').notNull(),project:text('project').notNull(),role:text('role').notNull(),content:text('content').notNull(),created:integer('created').notNull()},t=>[index('chats_owner_project').on(t.owner,t.project)]);
export const limits=sqliteTable('limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
